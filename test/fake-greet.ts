// Dummy module for flow demos: intentionally buggy. The fix is to correct
// this implementation so fake-greet.test.ts passes — do not edit the test.
export function greet(name: string): string {
  return `bye ${name}`;
}
